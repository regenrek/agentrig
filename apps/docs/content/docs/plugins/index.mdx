---
title: Plugins overview
description: What plugins contain and how they install.
icon: Package
---

# Plugins overview

A plugin is a portable unit of workflow logic. Each plugin ships files plus a `plugin.json` file that describes how to install them.

AgentRig plugin manifests are [Agent Plugins 1.0.0](https://agent-plugins.org/specification) conformant. AgentRig-specific fields live under `extensions["ai.agentrig"]`.

<Files>
  <Folder name="plugin-name" defaultOpen>
    <File name="plugin.json" />
    <Folder name="skills" defaultOpen>
      <Folder name="committer" defaultOpen>
        <File name="SKILL.md" />
        <File name="commit.sh" />
      </Folder>
    </Folder>
    <Folder name="scripts">
      <File name="lint.sh" />
    </Folder>
  </Folder>
</Files>

## What the CLI does

- Reads the plugin `plugin.json`.
- Validates target paths and hashes.
- Writes files to the target directory (`{{skillsDir}}` by default).
- Records the install in `.agentrig/manifest.json`.

Next, explore the full plugin structure and metadata options.
