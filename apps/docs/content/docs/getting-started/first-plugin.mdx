---
title: Your first plugin
description: Author and test a local plugin.
icon: Box
---

# Your first plugin

This page is for plugin authors.
If you want to consume plugins from registries, start with [Getting started](/getting-started).

A plugin is a folder with `plugin.json` and the files you want to distribute.

## Scaffold the plugin

```bash
agentrig plugin init acme.hello-skills
```

This creates a folder like:

```text
acme.hello-skills/
  plugin.json
  skills/hello-skills/SKILL.md
  README.md
```

The scaffolded manifest uses the Agent Plugins shape:

```json
{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "acme.hello-skills",
  "version": "0.1.0",
  "description": "A reusable AgentRig plugin.",
  "author": { "name": "Your Name" },
  "license": "MIT",
  "keywords": [],
  "extensions": {
    "ai.agentrig": {
      "displayName": "Hello Skills",
      "publicSkills": ["hello-skills"],
      "configSchema": { "type": "object", "additionalProperties": false, "properties": {} },
      "pluginDependencies": []
    }
  }
}
```

AgentRig plugin manifests are [Agent Plugins 1.0.0](https://agent-plugins.org/specification) conformant. AgentRig-specific fields live under `extensions["ai.agentrig"]`.
The extension is optional. Marketplace category, source provenance, scan data,
verification, approval, risk, support, ownership, and advisories are listing
control-plane metadata and do not belong in the published package.

<Callout title="Agent Plugins v1 hard cut">
New packages must use root `plugin.json` and `extensions["ai.agentrig"]`.
The previous `.plugin/plugin.json` and `x-agentrig` authoring shape is rejected.
Only exact immutable registry snapshots published before the cut remain readable
for pinned historical installs; publish the next version in the v1 layout.
</Callout>

## Add files

Add portable skills under `skills/<name>/SKILL.md` and optional portable MCP
configuration at root `mcp.json`. Put AgentRig-specific hooks, commands, agents,
rules, or other extension files under `ai.agentrig/`.
The scaffold already creates `plugin.json`. Update it after you add your real files:

```bash
$EDITOR plugin.json
```

## Test locally

Keep author testing on the local export surface instead of the public install command:

```bash
cd ..
agentrig plugin export --agent codex --pluginsDir ./acme.hello-skills --out dist/codex-marketplace
```

Next, learn about [`plugin.json`](/plugins/meta-json) and [`agentrig plugin`](/cli/plugin).
