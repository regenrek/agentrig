---
title: Your first plugin
description: Author and test a local plugin.
icon: Box
---

# Your first plugin

This page is for plugin authors.
If you want to consume plugins from registries, start with [Getting started](/getting-started).

A plugin is a folder with `.plugin/plugin.json` and the files you want to distribute.

## Scaffold the plugin

```bash
agentrig plugin init acme.hello-skills
```

This creates a folder like:

```text
acme.hello-skills/
  .plugin/plugin.json
  skills/hello-skills/SKILL.md
  README.md
```

The scaffolded manifest uses the Open Plugins shape:

```json
{
  "$schema": "https://agentrig.ai/schema/plugin.v1.json",
  "name": "acme.hello-skills",
  "version": "0.1.0",
  "description": "A reusable AgentRig plugin.",
  "author": { "name": "Your Name" },
  "license": "MIT",
  "keywords": [],
  "x-agentrig": {
    "displayName": "Hello Skills",
    "kind": "plugin",
    "configSchema": { "type": "object", "additionalProperties": false, "properties": {} },
    "pluginDependencies": []
  }
}
```

AgentRig plugin manifests are [Open Plugins 1.0.0](https://open-plugins.com/plugin-builders/specification) conformant. AgentRig-specific fields live under `x-agentrig`.

## Add files

Add skills, scripts, hooks, agents, or MCP config under the plugin directory.
The scaffold already creates `.plugin/plugin.json`. Update it after you add your real files:

```bash
$EDITOR .plugin/plugin.json
```

## Test locally

Keep author testing on the local export surface instead of the public install command:

```bash
cd ..
agentrig plugin export --agent codex --pluginsDir ./acme.hello-skills --out dist/codex-marketplace
```

Next, learn about [`.plugin/plugin.json`](/plugins/meta-json) and [`agentrig plugin`](/cli/plugin).
