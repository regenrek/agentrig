---
title: Getting started
description: Install AgentRig, connect to registries, and install your first provider plugin.
icon: Rocket
---

# Getting started

AgentRig starts with a simple consumer flow:

1. initialize config
2. browse available plugins
3. inspect a plugin if needed
4. install it as a native provider plugin

## Official registry flow

<Steps>
  <Step>
    <h3>Install agentrig</h3>
    <p>Install the CLI once so you can use the same plugin workflow in any repo.</p>

    ```bash
    npm install -g agentrig
    ```
  </Step>
  <Step>
    <h3>Initialize config</h3>
    <p>Seed a minimal config with the official registry.</p>

    ```bash
    agentrig init
    ```
  </Step>
  <Step>
    <h3>Browse the official registry</h3>
    <p>See what plugins are available before you install anything.</p>

    ```bash
    agentrig list --available
    ```
  </Step>
  <Step>
    <h3>Inspect the plugin</h3>
    <p>Optional, but useful if you want to review dependencies, files, and trust before installing.</p>

    ```bash
    agentrig view core-committer
    ```
  </Step>
  <Step>
    <h3>Install it as a provider plugin</h3>
    <p>Choose the provider you actually use in Claude Code, Codex, or Cursor.</p>

    ```bash
    agentrig plugin install codex core-committer
    ```
  </Step>
</Steps>

## Third-party registry flow

Third-party registries are explicit.
Add the registry first, then install plugins from it with `registryAlias/plugin-name`.

```bash
agentrig registry add georg https://georg.dev/agentrig
agentrig list --available --registry georg
agentrig plugin install cursor georg/ts-master-plugin
```

## Direct URL or local file flow

Direct `.plugin/plugin.json` URLs and local `.plugin/plugin.json` files are treated as unlisted sources.
Install them only when you trust the source:

```bash
agentrig plugin install codex ./hello-skills/.plugin/plugin.json --yes
```

## Where to go next

- Want the config details? Read [`agentrig init`](/cli/init) and [Project config](/configuration/project).
- Want to add a third-party source? Read [`agentrig registry`](/cli/registry).
- Want provider-native installs? Read [Integrations](/integrations).
- Want to author a plugin instead of consuming one? Start with [Your first plugin](/getting-started/first-plugin).
