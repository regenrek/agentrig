---
title: Extending rigs
description: Build layered rig hierarchies.
icon: GitMerge
---

# Extending rigs

Rigs can extend other rigs using the `extends` array.
The CLI resolves plugins in order and de-duplicates while preserving order.

Example:

```json
{
  "rigs": {
    "core": {
      "plugins": [
        "agentrig/agentrig.core-committer@0.1.0",
        "agentrig/agentrig.security-check@0.1.0"
      ]
    },
    "website": { "extends": ["core"], "plugins": ["agentrig/community.typescript@0.1.0"] }
  }
}
```

When you apply `website`, agentrig installs:

1. `agentrig/agentrig.core-committer@0.1.0`
2. `agentrig/agentrig.security-check@0.1.0`
3. `agentrig/community.typescript@0.1.0`

This keeps shared foundations in one place.
