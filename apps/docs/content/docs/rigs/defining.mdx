---
title: Defining rigs
description: Configure rig profiles in agentrig.config.json.
icon: Settings2
---

# Defining rigs

Rigs are stored under the `rigs` key in your config file.

```json
{
  "rigs": {
    "core": {
      "plugins": [
        "agentrig/agentrig.core-committer",
        "agentrig/agentrig.security-check"
      ]
    },
    "tauri-agentic": {
      "extends": ["core"],
      "plugins": ["agentrig/community.typescript"]
    }
  },
  "defaultRig": "core"
}
```

## Fields

- `plugins` - list of canonical install refs to install.
- `extends` - list of parent rigs to merge in order.
- `defaultRig` - used when you run `agentrig rig apply <provider>` without a rig name.
