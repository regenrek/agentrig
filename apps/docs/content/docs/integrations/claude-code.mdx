---
title: Claude Code
description: Install resolved packs as Claude Code plugins.
icon: Sparkles
---

# Claude Code integration

Consumers install resolved packs into Claude with the top-level plugin command:

```bash
agentrig plugin install claude core-committer
agentrig plugin install claude georg/ts-master-pack
```

Claude installs use Claude's native marketplace commands under the hood.

## Uninstall from Claude Code

```bash
agentrig plugin uninstall claude core-committer
```

## What AgentRig does

```text
claude plugin marketplace add <generated-marketplace-dir>
claude plugin install <plugin>@<marketplace> --scope user|project
```

By default, `--scope auto` resolves to `workspace` for Claude.
Use `--scope personal` when you want a user-level install instead.

## What Claude plugins include

Claude plugins can include:

- `skills/`
- `commands/`
- `agents/`
